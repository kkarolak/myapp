<% if user_signed_in? && current_user.role == 'admin' %>
  <h1> Admin Panel</h1>
<% end %>
<% if (user_signed_in? && current_user.role != 'admin') || !user_signed_in? %>
  <h1> Home Page</h1>
<% end %>